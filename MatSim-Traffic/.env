PYTHONPATH=.
PYTHON_MIDDLEWARE_LISTENER=http://localhost:5000
FIWARE_URL=http://localhost:1026
TRAFFIC_CONTEXT_URL=http://host.docker.internal:3004/traffic-context.jsonld
OUTPUT_URL=http://localhost:8000/output/ # Output directory where the simulation results will be stored and served from. This should match the URL used in the web app to fetch the results.
OUTPUT_PORT=8000
JAVA_LISTENER=http://localhost:8080/ # The URL where the Java listener is running, which will receive notifications from FIWARE about road closures.
DB_PATH=./live_information.db