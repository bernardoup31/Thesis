import EVDashboard from "@/components/EVDashboard";

export default function ElectricVehiclesPage() { // TODO - Implement a initial dashboard that shows the map and where the user can select a road and add a new electric pole, with the information of available energy, vehicles waiting, etc. This will be the first step to then implement the electric vehicles simulation, where we can have a more complex dashboard with the state of the electric poles, the vehicles, etc.
  return (
    <EVDashboard />
  );
} 